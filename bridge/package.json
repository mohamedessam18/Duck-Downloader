{
  "name": "duck-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Native messaging host that connects the Duck extension to the local engine.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "install-host": "node scripts/install-host.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
